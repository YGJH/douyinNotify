[project]
name = "douyinnotify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "multithreading>=0.2.0",
    "playwright",
    "requests>=2.32.5",
]
